module PACEMAKER:
input INT(integer), HEART_BEAT;
output BPM(integer);
    signal RESET, TIME_OUT in
        run COORDINATOR
    ||
        run TIMEOUT
    ||
        run FREQ_CONV
    end signal
end module