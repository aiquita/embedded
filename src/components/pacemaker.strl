module PACEMAKER:
input INT(integer), HEART_BEAT;
output TIME_OUT, BPM(integer);
    signal RESET in
        run COORDINATOR
    ||
        run TIMEOUT
    ||
        run FREQ_CONV
    end signal
end module